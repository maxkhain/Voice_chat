# Build Requirements for HexChat Executable
# Install with: pip install -r requirements-build.txt

pyinstaller>=6.0.0

# Runtime dependencies (should already be installed)
flet>=0.23.0
pygame>=2.6.0
sounddevice>=0.4.6
numpy>=1.26.0
cryptography>=41.0.0
pywin32>=306
